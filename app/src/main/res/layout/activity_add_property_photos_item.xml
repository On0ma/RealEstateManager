<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/property_picture"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:adjustViewBounds="true"
        android:scaleType="centerCrop"
        android:layout_marginEnd="8dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="@id/property_picture"/>

    <EditText
        android:id="@+id/property_picture_legend"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:padding="4dp"
        android:background="#66000000"
        android:textSize="12sp"
        android:textColor="#eeeeee"
        tools:text="Description de la photo"
        app:layout_constraintBottom_toBottomOf="@id/property_picture"
        app:layout_constraintStart_toStartOf="@id/property_picture"
        app:layout_constraintEnd_toEndOf="@id/property_picture"/>

    <ImageButton
        android:id="@+id/property_picture_delete"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/delete"
        android:layout_marginTop="5dp"
        android:layout_marginEnd="5dp"
        app:layout_constraintTop_toTopOf="@id/property_picture"
        app:layout_constraintEnd_toEndOf="@id/property_picture"
        />

</androidx.constraintlayout.widget.ConstraintLayout>